# 剧本杀项目专用规则（执行版）

你正在开发一个“剧本杀 Telegram Bot”。请严格遵守以下项目规则。

## 一、回复与输出风格

1. 所有对外文案默认使用中文简体。
2. 回复优先给“可直接执行”的结果，少空话。
3. 修改代码时先说明改动目标，再给具体变更点。

## 二、目录与职责

1. `handlers/` 只处理入口，不写复杂业务。
2. `services/` 负责剧本杀核心规则与流程推进。
3. `storage/` 或 `repositories/` 负责读写数据。
4. `adapters/` 负责 Telegram API 与外部依赖。

## 三、剧本杀业务硬规则

1. 必须按状态机推进：等待中 -> 进行中 -> 投票中 -> 已结束。
2. 不允许越级状态切换；每次切换必须有前置条件校验。
3. 发身份必须私聊；群聊不得出现任何身份泄露内容。
4. 投票和结算逻辑要幂等，重复请求不能重复生效。
5. 任意异常都不能导致整局崩溃，至少要可恢复或可结束。

## 四、指令与交互

1. 新增指令时，必须给出用途、权限、可执行阶段。
2. 非法操作必须返回明确提示（为什么失败、下一步做什么）。
3. callback query 必须及时应答，避免 Telegram 端卡 loading。

## 五、安全与配置

1. 所有密钥仅从 `.env` 读取，禁止硬编码。
2. 禁止提交 `.env`、日志密钥、数据库凭据。
3. 日志里禁止打印完整 token、用户隐私、完整身份表。

## 六、代码与测试要求

1. 新增业务逻辑至少覆盖一条正常流程和一条异常流程验证。
2. 修复 bug 必须补一个回归验证点（测试或可复现步骤）。
3. 复杂逻辑拆分函数，避免超长函数和神对象。

## 七、改动优先级

1. 优先保证“能完整跑完一局游戏”。
2. 优先修复影响开局、发身份、投票、结算的缺陷。
3. 在不破坏现有流程前提下再做体验优化。
